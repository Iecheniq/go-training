package operations

import (
	"fmt"
)

func Sum(x, y int) {
	result := x + y
	fmt.Println(result)
}

func Sub(x, y int) {
	result := x - y
	fmt.Println(result)
}

func Mul(x, y int) {
	result := x * y
	fmt.Println(result)
}

func Div(x, y int) {

	result := x / y
	fmt.Println(result)
}
